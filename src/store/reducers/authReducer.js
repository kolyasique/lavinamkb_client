export default function authReducer(state, action) {
  switch (action.type) {
    case 'AUTHENTIFICATION':
      return state;
    case 'REGISTRATION':
      return state;
    default:
      return state;
  }
}
